{
  "name": "@ryvra/policy-risk",
  "version": "0.1.0",
  "private": true,
  "description": "Ryvra Policy Risk decision layer baseline",
  "packageManager": "pnpm@10.16.0",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.spec.ts",
    "validate:versions": "node scripts/validate-version-consistency.mjs",
    "verify:staging:http": "node -e \"console.log(\\\"See docs/staging-http-mode-verification.md for staging verification contract.\\\")\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.1",
    "typescript": "^5.7.2"
  }
}
